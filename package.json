{
  "name": "gestureup",
  "version": "10.6.0",
  "private": true,
  "description": "Node tests for the GestureUp extension. The extension itself is plain files loaded by the browser and has no build step or dependencies.",
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  }
}
